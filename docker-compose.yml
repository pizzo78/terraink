services:
  terraink:
    build:
      context: .
      dockerfile: Dockerfile
    image: terraink:latest
    container_name: terraink
    ports:
      - "${APP_PORT:-7203}:80"
    restart: unless-stopped
